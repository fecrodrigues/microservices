version: "3.7"

services:

  frontend:
    image: fcrodrigues/frontend-react
    ports: 
      - 3001:3000

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports: 
      - 80:80
    depends_on: 
      - frontend
